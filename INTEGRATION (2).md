# CONTUR → редактор мнемосхем: что мы отдаём и как это рисовать

Документ для тех, кто принимает наши `*.json`. Здесь описано, **как схема
выглядит у нас в приложении после загрузки Lua, открытия PDF и разметки** —
и какой элемент файла за что отвечает. Если рисовать по этим правилам,
в редакторе получится та же картинка.

Формат самого файла (плоский массив, ключи, `null`, порядок полей) описан
в `PROJECT.md`, §6.2. Здесь — про смысл и внешний вид.

---

## 1. Откуда берутся данные

| Источник | Что даёт |
|---|---|
| `main.io.lua`, `main.wago.lua` | устройства контроллера: имя (`LA_TANK1V1`), тип (`V`, `LS`, `TE`…), артикул, описание |
| `main.objects.lua` | технологические объекты (танки, линии, CIP), их операции и состояния |
| PDF Eplan | векторный чертёж: линии, прямоугольники, кривые, текстовые обозначения |
| Модель YOLO | рамки устройств на чертеже (`valve` / `sensor` / `pump`) |

Дальше конвейер связывает одно с другим: замкнутые контуры на чертеже
получают имена техобъектов, текстовые обозначения (`-V1`, `LS2`) — имена
устройств из Lua. Результат этой связи и уезжает в файл.

Главное, что стоит понимать: **чертёж и наши данные — два разных слоя.**
Линии — это перерисованный PDF. Кружки, рамки и подписи — то, что мы
про этот чертёж поняли.

---

## 2. Система координат

- Единица — **пункт PDF**, он же пиксель холста. Масштабировать ничего
  не нужно, коэффициентов нет.
- Начало — **левый верхний угол листа**, ось X вправо, ось Y **вниз**.
- Размер холста равен размеру листа: A0 — 3368×2384, A1 — 2392×1695.
  Лист целиком помещается в холст 5000×5000.
- **Процентов в файле нет намеренно.** Импортёр умножает строку `"84.9%"`
  на `CANVAS_W = CANVAS_H = 5000`, а холст квадратный, лист — нет:
  A0 растянуло бы с 1.41:1 до 1:1.

**Точка привязки `x, y` зависит от типа** — здесь первая редакция документа
была неточна, «центр у всех» верно не для всего:

| Тип | Что означают `x, y` |
|---|---|
| `line` | середина отрезка (сама геометрия — в `x1, y1, x2, y2`) |
| `circle` | **центр** круга; габарит `w = h = 2·radius` |
| `text` | **левый верхний угол** строки |

У круга центр — потому что так устроено наше окно и так задан `radius`.
Если у вас `x, y` круга это левый верхний угол габарита — вычтите радиус:
`x − radius`, `y − radius`. Именно из-за этого расхождения кружки в первом
импорте стояли рядом с символами, а не на них.

У текста привязка — левый верхний угол строки (у нас это позиция
`QGraphicsTextItem`, а она задаёт именно угол). Сдвиги из §4.4 и §4.5
(+8, −10) и (−30, −15) — это сдвиги **угла строки** относительно центра
устройства или контура.

---

## 3. Слои и порядок отрисовки

В нашем окне (значения `z` из `scene_painter.py`):

| Слой | z | Что это |
|---|---|---|
| разметка чертежа | −1 | линии, перерисованные из PDF |
| рамки техобъектов | 0 | прозрачные прямоугольники |
| имена контуров и подписи устройств | 2 | текст поверх чертежа |
| устройства | 3 | кружки |

**В файле элементы уже идут в этом порядке**: рамки контуров → линии
чертежа → кружки устройств → тексты. Достаточно рисовать подряд, ничего
не сортируя.

---

## 4. Что чем рисуется

### 4.1 Линия чертежа — элемент `line` с полем `color`

Перерисованный PDF, самый многочисленный вид элементов.

| | |
|---|---|
| Геометрия | `x1, y1, x2, y2` — концы отрезка. `x, y` — середина, `w, h` — габарит; **для отрисовки они не нужны** |
| Цвет | `borderColor` (`#e53935` или `#1e88e5`) |
| Толщина | `stroke_width`, в пунктах, прямо из чертежа |
| Смысл | `color: "blue"` — трубопровод, `color: "red"` — контур устройства, найденного моделью |
| Ссылки | `line_id` — наш номер сегмента, у красных ещё и `device_name` |

Кривые из PDF уже разложены на отрезки, прямоугольники — на четыре стороны.
Ничего замкнутого рисовать не надо, только отрезки.

### 4.2 Рамка техобъекта — элементы `line` с полем `contour: true`

Четыре стороны прямоугольника, охватывающего танк, линию или шкаф. У нас это
рамка пером 2 пт с полупрозрачной заливкой (альфа 50 из 255) того же цвета.

| | |
|---|---|
| Отличие от чертежа | есть `contour: true` и `tech_object`, **нет поля `color`** |
| Цвет | `borderColor`, выбирается по имени техобъекта из палитры десяти цветов |
| Заливка | в файле не передаётся; если рисуете — тот же цвет с прозрачностью ~20 % |

Прямоугольник собран из линий не от бедности: тип `line` у вас заведомо
рисуется, а `rect` в вашей выгрузке не встречается. Если у вас есть свой
прямоугольник — соберите его из этих четырёх сторон, они идут подряд.

### 4.3 Устройство — элемент `circle`

Сопоставленное устройство: у нас это кружок с чёрной обводкой 1 пт.

| | |
|---|---|
| Геометрия | центр `x, y` и `radius`; `w = h = 2·radius` — габарит того же круга |
| Цвет | `bg` и `borderColor` — по типу устройства (таблица ниже) |
| Размер | из рамки, найденной моделью; ограничен пределами 0.5–1.5 от обычного символа листа (около 33 пт на A0) |
| Смысл | `lua_name` — полное имя из контроллера, `pdf_name` — обозначение с чертежа, плюс `tech_object`, `device_type`, `confidence`, `descr`, `article` |

Цвета по типу устройства — те же, что у нас в окне:

| Тип | Цвет | Тип | Цвет |
|---|---|---|---|
| `V` клапан | `#e74c3c` | `LS` уровень (дискретный) | `#8e44ad` |
| `M` насос | `#34495e` | `LT` уровень (аналоговый) | `#9b59b6` |
| `DI` / `DO` | `#3498db` / `#2980b9` | `TE` температура | `#1abc9c` |
| `AI` / `AO` | `#2ecc71` / `#27ae60` | `PT` / `PC` давление | `#f39c12` / `#7f8c8d` |
| `QT` | `#d35400` | `FQT` расход | `#c0392b` |
| `GS` | `#16a085` | `SB` кнопка | `#2c3e50` |
| `HL` / `HLA` лампа | `#f1c40f` / `#f39c12` | `G` | `#95a5a6` |

Остальные типы — чёрный `#000000`.

### 4.4 Подпись устройства — элемент `text` без `contour`

| | |
|---|---|
| Строка | в полях `text` **и** `label` — продублирована: какое из них читает ваш редактор, по вашей выгрузке не понять |
| Содержание | обозначение с чертежа, а при неполной уверенности модели — она в скобках: `-V1 (0.9)` |
| Положение | `x, y` уже со сдвигом (+8, −10) от центра устройства — как в нашем окне |
| Оформление | у нас Arial 8, чёрный текст на белой подложке (альфа 200); в файле `font_size: 8` и `borderColor: "#000000"` |
| Связь | `lua_name` и `tech_object` — к какому устройству относится |

### 4.5 Имя техобъекта — элемент `text` с `contour: true`

То же самое, но крупнее (`font_size: 10`, у нас полужирный) и цветом своего
контура. Положение — у центра контура со сдвигом (−30, −15).

---

## 5. Почему у устройства две фигуры

Частый вопрос: рядом с прямоугольником стоит кружок.

Это не сбой отрисовки, а два слоя, которые у нас лежат друг на друге:

- **прямоугольник** — контур самого устройства, перерисованный с чертежа
  (те самые линии с `color: "red"`);
- **кружок** — наш маркер: он несёт имя из контроллера, тип, артикул
  и описание, которых на чертеже нет.

Что с этим можно сделать:

1. **Ничего.** У нас в окне так и выглядит: кружок сидит на символе.
2. **Не рисовать красные линии** — пропускать элементы с `color: "red"`.
   Символ пропадёт, останется кружок с именем.
3. **Попросить у нас файл без них** — выгрузим с `CONTUR_HMI_LINES=pipes`,
   он ещё и вдвое легче: 3259 элементов вместо 6178.

Если кружок стоит **ровно вправо-вниз на свой радиус** от символа — дело
не в этом, а в точке привязки: у нас `x, y` круга это центр (§2). Вычтите
радиус. Если же смещение произвольное и не равно радиусу — скажите, тогда
это наша геометрия: центр берётся из скопления красных линий, а оно иногда
прихватывает соседний отвод.

---

## 6. Что мы можем менять по просьбе

Переключается на нашей стороне, файл вы получаете уже готовым:

| Что | Значение | Зачем |
|---|---|---|
| `CONTUR_HMI_LINES` | `pipes` | без контуров устройств: 3259 элементов вместо 6178, файл 2.4 МБ вместо 4.6 |
| | `none` | вообще без чертежа: 631 элемент (рамки, устройства и подписи), 0.5 МБ |
| `CONTUR_HMI_CONTOURS` | `0` | без рамок и имён техобъектов |
| `CONTUR_HMI_LABELS` | `0` | без подписей устройств |
| `CONTUR_HMI_SHAPES` | `class` | клапаны и насосы — `polygon`, датчики — `circle` |
| `CONTUR_HMI_GROUPS` | `1` | техобъект становится группой `group` — см. §6.1 |
| `CONTUR_HMI_SCALE` | число | множитель координат, если холст мельче листа |

Отдельно: имена полей у нас в змеином регистре (`stroke_width`, `font_size`)
и `borderColor` вместо `strokeColor`. Если удобнее получать сразу в ваших
именах — скажите, будем дублировать поля в выгрузке, менять формат целиком
для этого не нужно.

### 6.1 Режим групп (`CONTUR_HMI_GROUPS=1`)

Что в файле меняется:

- техобъект становится элементом `group` с `composition: true`, полупрозрачной
  заливкой и пунктирной рамкой — **отдельных линий рамки в этом режиме нет**,
  рамкой служит сама группа;
- **детьми группы становятся устройства, их подписи и имя техобъекта.**
  Всё, что относится к объекту, двигается вместе с ним;
- **связь двусторонняя**: у ребёнка `parentKey` = ключ группы, у группы
  ключ ребёнка в массиве `children`. Ссылок на отсутствующие элементы нет;
- **координаты детей локальные**, от левого верхнего угла группы. Устройство
  на холсте = `группа.x + ребёнок.x`. Подписи пересчитываются вместе
  с устройствами и сохраняют свой сдвиг (+8, −10).

Числа в этом режиме: контрольный лист A0 — 6076 элементов (30 групп,
5547 линий чертежа, 233 устройства, 266 подписей и имён); лист 13
mozzarella — 1129 (5 групп, 1049 линий, 35 устройств, 40 подписей и имён).

Группа заводится только для техобъекта, у которого на листе есть
сопоставленные устройства: на A0 это 30 контуров из 33. Имена остальных
трёх и устройства без своего контура остаются верхнеуровневыми,
с абсолютными координатами.

Про `composition`: у группы это `true`, как в выгрузке вашего же редактора.
Ваш импортёр ждёт в этом поле массив и подставляет `[]` — на связь
родитель-ребёнок это не влияет, она держится на `children` и `parentKey`.

---

## 7. С чем сверяться при приёмке

Контрольный лист A0 (`BN1-МОЛОКОХРАНИЛИЩЕ`, 3368×2384):

| Вид элемента | Сколько |
|---|---|
| линии чертежа | 5547 |
| стороны рамок контуров | 132 (33 контура × 4) |
| устройства | 233 |
| подписи устройств | 233 |
| имена контуров | 33 |
| **всего** | **6178**, файл 4.6 МБ |

Лист 13 проекта mozzarella (A1, 2392×1695): 1049 линий чертежа, 20 сторон
рамок (5 контуров), 35 устройств, 35 подписей и 5 имён — всего 1144.

Если в редакторе получилось столько же элементов, а круги остались кругами —
файл прочитан правильно.

---

## 8. Чего в этом файле нет

- **Текстов самого чертежа** (обозначения Eplan, номера позиций). В нашей
  разметке они есть, в выгрузку не попадают — скажите, если нужны.
- **Трубопроводов как связных цепочек** и **графа соединений**
  «устройство ↔ труба ↔ устройство». Это есть в нашем втором формате,
  `*.plant.json` (`PROJECT.md`, §6.3): там же точки сопряжения, полная
  разметка листа и техобъекты деревом. Тот формат не для холста — для данных.
- **Состояний устройств в операциях** (открыт или закрыт на каждом шаге
  мойки). У каждого элемента есть список `states` с одним состоянием
  «Нормальное»; наполнить его нашими операциями можем, если решите,
  как это должно выглядеть у вас.
